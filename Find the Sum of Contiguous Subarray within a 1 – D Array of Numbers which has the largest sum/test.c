
#include"stdio.h"
int second_largest(int * ,int );
int largest(int *,int );
void main()
{
    int i,j,sum=0,size,pair[];
    printf("Enter the size of array\n");
    scanf("%d",&size);
    int arr[size];
    printf("Enter the elements of array\n");
    for(i=0;i<size;i++)
    {
        fflush(stdin);
        scanf("%d",&arr[i]);
    }

    pair = find_pair(arr,size);

}




int find_pair(arr[],size)
{
    int i,j,num1,num2,sum1,sum2;
    for(i=0;i<size;i++)
    {
        for
    }
}
